# MySQL external connection for development/preview
# This file is loaded in addition to .env.dev by the dev:mysql script.
# Fill in the placeholders below with your external DB info.

# Examples:
# - Basic:            mysql://USERNAME:PASSWORD@DB_HOST:3306/DB_NAME
# - With SSL (common): mysql://USERNAME:PASSWORD@DB_HOST:3306/DB_NAME?sslaccept=strict

# Required: replace the placeholders with real values
DATABASE_URL=mysql://root:Hykymysqlyk@2024@rm-2ze3pg2vy2hv479vlpo.mysql.rds.aliyuncs.com:3306/happy_server

# Keep other settings (PORT, S3, METRICS, etc.) from .env.dev.
# Only the database URL is overridden here.
